rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // WARNING: These rules are extremely insecure and allow anyone to read, write, and delete 
    // any data in your database. Use only for temporary testing and never in production.
    match /{document=**} {
      allow read, write: if true;
    }

  }
}
